export interface Student { 
        id: string;
        name: string;
        mobile: number,
        city: string,
        postal_code: string;
        address_lin1: string;
        address_lin2: string;
}